# Docker

Como el despliegue es parte del proyecto, aquí deben incluirse:

- `Dockerfile`(s)
- `docker-compose.yml`
- Variables de entorno (ver `.env.example`)
- Instrucciones de cómo levantar el sistema (ver el `README.md` de la raíz)
