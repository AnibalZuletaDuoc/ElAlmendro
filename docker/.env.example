# Las variables de entorno del proyecto viven en un unico archivo, en la
# raiz del repositorio: .env.example
#
# Para configurar tu entorno local:
#     cp .env.example .env
#
# docker compose lee ese mismo archivo mediante --env-file .env, que ya
# viene configurado en los scripts npm run infra:* de la raiz.
